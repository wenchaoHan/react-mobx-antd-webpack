import renderMore from './renderMore';
import formatVal from './formatVal';
import validator from './validator';
import wrap from './wrap';
import validateBone from './validateBone';
import hoistStatic from './hoistStatic';

// export { default as controlled } from './controlled'; // zhenchao.sun

export default {
  renderMore,
  formatVal,
  validator,
  wrap,
  validateBone,
  hoistStatic,
};
